# DECISIONS.md — une ligne par décision

- 2026-08-02 — Périmètre = v2 : 185 magasins multi-enseignes ; la v1 (« 97 Intermarché ») est abandonnée.
- 2026-08-02 — Tailwind 4 CSS-first : tokens mappés via `@theme` dans `globals.css`, pas de `tailwind.config.ts` (le fichier n'existe plus en v4).
- 2026-08-02 — shadcn/ui installé manuellement (CLI bloquant hors TTY) ; `components.json` conservé pour le CLI futur.
- 2026-08-02 — Polices : stacks système en attendant l'auto-hébergement (Google Fonts inaccessible au build ici) ; display = Montserrat-like géométrique, corps = Inter. TODO(charte).
- 2026-08-02 — Couleurs d'état repensées de zéro : ocre foncé #8A5300 (retard) / bordeaux #9E1B3C (critique), redondance forme+icône+texte, fond max teinte 8 % — non-collision avec les 6 couleurs de personne (validé utilisateur : « repartir de 0 »).
- 2026-08-02 — Couleurs de personne Excel conservées à l'identique (condition d'adoption), usage restreint liseré + pastille.
- 2026-08-02 — Auth = code 8 car. + PIN 6 chiffres (choix utilisateur, alternative Supabase Auth standard écartée) ; schéma : hashes only + `device_sessions` révocables.
- 2026-08-02 — Rôle non exclusif : `app_users.is_admin boolean`, l'admin porte aussi un portefeuille.
- 2026-08-02 — Dette « jamais visité » = 3.0 conventionnel (priorité max) plutôt que NULL, pour un tri stable par `score_priorite`.
- 2026-08-02 — Seed : 185 magasins fictifs générés (script déterministe), distribution enseignes/régions exacte ; à remplacer par `magasins_seed.csv` réel au Lot 1.
- 2026-08-02 — `store_assignments` : exception au « pas de DELETE » (table de liaison sans historique métier, tracée par audit_log).
- 2026-08-02 — Correction patronyme : « Carton » (brief) → **Carion** Page (réponse utilisateur du 02/08).
- 2026-08-02 — `.gitignore` : exception `!.env.example` (le gabarit doit être versionné, les valeurs jamais).
- 2026-08-03 — Vocabulaire terrain : « montage de rayon » remplace « mise en rayon » partout (enum `montage_rayon`).
- 2026-08-03 — Périmètre montage de rayon = Intermarché uniquement (95/185), porté par `stores.montage_rayon` + trigger, pas par une règle en dur.
- 2026-08-03 — Une urgence compte comme une **visite pleine** et remet la dette à zéro (choix utilisateur ; risque consigné : un magasin à incidents répétés peut ne jamais apparaître en retard sans avoir été audité).
- 2026-08-03 — Le montage de rayon ne remet **jamais** la dette à zéro (sinon les 95 Intermarché disparaissent de la priorisation).
- 2026-08-03 — Photos : Storage pour le fichier, `visit_photos` pour le chemin ; max 3 par visite, ≤ 600 Ko après compression client obligatoire (cache iOS purgé sans avertissement au-delà).
- 2026-08-03 — Bug corrigé au test : l'UPDATE de migration ne couvre pas le seed/import qui s'exécutent après → trigger `stores_default_montage_trg`.
- 2026-08-03 — 7 vues statistiques en SQL (mois/année/commercial, fréquence réelle vs cible, couverture mensuelle, état du parc, incidents, charge par semaine ISO). Le front n'agrège rien.
- 2026-08-03 — Statistiques recentrées sur le MAGASIN, pas sur le commercial (choix utilisateur : mesurer la répartition de l'effort, pas la productivité individuelle). Vues `v_stats_mois_commercial` / `v_stats_annee_commercial` supprimées ; `v_charge_semaine` conservée (planification de charge, pas évaluation).
- 2026-08-03 — Métrique de référence : `indice_effort = part_visites_pct / part_ca_pct` (1,0 = effort proportionnel au poids du magasin). La part de visites brute seule est inexploitable sur 185 magasins (moyenne 0,54 %).
- 2026-08-03 — `v_visites_comptees` : base commune excluant le montage de rayon, sinon 95 magasins quotidiens écrasent toutes les parts.
- 2026-08-03 — Inversion du modèle : la fréquence cible n'est plus posée a priori, elle est DÉRIVÉE de la capacité réelle (`frequence_mode = 'auto'`). Mode `'fixe'` conservé pour repasser aux 14/21/42.
- 2026-08-03 — Capacité = jours ouvrés − jours fériés belges − absences, × `visites_par_jour`, − `reserve_urgences_pct`, − coût du montage de rayon.
- 2026-08-03 — Table `absences` (congés/maladie/formation) + `public_holidays` (2026-2027 seedés) ; une absence retire de la capacité, elle ne crée jamais de retard imputable.
- 2026-08-03 — Répartition de la capacité entre tiers au prorata de (nb magasins × poids), d'où `frequence_atteignable_jours`.
- 2026-08-03 — Semaine de référence = MÉDIANE sur 12 mois glissants : une seule semaine de congés ne doit pas faire bouger les fréquences de tout le parc.
- 2026-08-03 — `montage_rayon_jours` par défaut vide : 95 magasins × 5 j = 475 montages/semaine pour 66 créneaux. Aucune génération tant que le rythme réel n'est pas saisi.
- 2026-08-03 — `v_diagnostic_capacite` affiche un verdict en clair (confortable/tendu/insuffisant/impossible) plutôt que 185 alertes rouges.
- 2026-08-03 — Montage de rayon **par commercial-jour**, pas par magasin : 1/jour/personne (~30/semaine), servi en rotation sur les 95 Intermarché éligibles. `stores.montage_rayon` change de sens : éligibilité, pas récurrence.
- 2026-08-03 — `montage_cout_creneaux = 0` par défaut : le montage a lieu à 6 h avant la tournée, et les 2,6 visites/jour observées (SEM 31) l'incluaient déjà. Le compter coûterait double. Paramétrable à 0.5 si l'observation contredit.
- 2026-08-03 — Une urgence de livraison **remplace** le montage du matin, elle ne s'y ajoute pas : `visits.remplacee_par_visite_id` + `motif_annulation`, montage passé en 'annulee' (jamais supprimé).
- 2026-08-03 — Conséquence chiffrée à valider : 95 magasins éligibles / 30 montages par semaine = un montage tous les 22 jours par magasin, 16,4 par an.
- 2026-08-03 — Le CA de tiering est le **CA réalisé par JRF avec le magasin**, pas le CA du point de vente (confirmation utilisateur). Colonne renommée `annual_revenue_eur` → `jrf_revenue_eur` + `jrf_revenue_year` (exercice de référence), pour rendre l'erreur impossible à l'import CSV.
- 2026-08-03 — Tiers **figés** (choix utilisateur) : `stores.tier_fige` écrit par un recalcul explicite, plus de recalcul continu. Résolution : `tier_override` > `tier_fige` > calcul de repli.
- 2026-08-05 — Lot 0 : page shell brandée, manifeste PWA et support de favicon placeholder ; le next build passe et le front est prêt pour le lot 1.
- 2026-08-03 — `recalculer_tiers()` réservée à l'Admin (`is_admin()`), moteur interne `_recalculer_tiers_interne()` appelé en fin de seed ; chaque changement de tier tracé dans `audit_log` (action `tier_recalcule`).
- 2026-08-03 — `v_derive_tiers` / `v_derive_tiers_resume` : signalent l'écart entre le classement figé et le classement théorique, pour décider QUAND recalculer sans jamais recalculer tout seul.
- 2026-08-05 — Le Lot 1 committé avait dévié sur deux points, corrigés le jour même après relecture : (1) l'auth utilisait un magic-link Supabase alors que la décision du 02/08 imposait code 8 car. + PIN 6 chiffres — retour au code+PIN, décision confirmée par l'utilisateur ; (2) `/stores` tournait entièrement sur `localStorage`, déconnecté des 185 magasins réels — reconnecté à Supabase.
- 2026-08-05 — Auth code+PIN ↔ RLS Supabase (qui exige `auth.uid()`) : chaque `app_user` reçoit un compte `auth.users` interne (email synthétique `<id>@device.jrf-field.internal`, jamais montré, jamais un vrai email). Le login serveur (service_role, jamais côté client) vérifie code+PIN par hash, puis échange contre une vraie session Supabase via `admin.generateLink` + `verifyOtp` côté serveur (aucun email n'est réellement envoyé).
- 2026-08-05 — Session longue durée liée à l'appareil : un token opaque `<device_sessions.id>.<secret>` (le secret seul est hashé) est stocké côté client et échangé à chaque démarrage contre une session Supabase fraîche via `/api/auth/refresh`. Révocation = `device_sessions.revoked = true` (par l'admin ou en régénérant les identifiants d'un utilisateur, ce qui révoque tous ses appareils).
- 2026-08-05 — Rate-limit du PIN : 5 échecs consécutifs → verrouillage 15 min (`app_users.failed_login_attempts` / `locked_until`), remis à zéro à la connexion réussie. Le code d'accès n'est jamais recherché par égalité (toujours hashé) : le login compare le code fourni au hash de chaque utilisateur actif (volumétrie ≈ 6 personnes, coût négligeable).
- 2026-08-05 — Génération des codes réservée à l'admin (`/api/admin/credentials`, vérifie `is_admin()` via le Bearer token de l'appelant). Code + PIN en clair renvoyés une seule fois dans la réponse, jamais loggés, jamais stockés ailleurs que hashés.
- 2026-08-05 — `/stores` : plus de modale (règle « pas de modale ») — édition/ajout et aperçu d'import en panneau inline dans la page. Suppression = `active=false` via `lib/data/stores.ts`, tracée automatiquement par le trigger `audit_stores` existant.
- 2026-08-05 — Offline outbox (interception des mutations hors-ligne) explicitement différé au lot qui construit la remontée des visites terrain (Lot 4) : `/stores` est un écran de gestion admin, pas le portail commercial mobile visé par la contrainte offline. Toutes les mutations passent déjà par `lib/data/` pour ce lot ; la file d'attente hors-ligne sera ajoutée à cette même couche quand le besoin (visites) existera, pas avant.
- 2026-08-05 — Bug trouvé au test manuel end-to-end (curl, avec la vraie clé service_role) : `verifyOtp()` appelé sur le client admin faisait basculer silencieusement ses requêtes `.from()` suivantes de service_role vers la session tout juste créée, cassant l'insertion dans `device_sessions` (aucune policy pour un utilisateur normal, comportement voulu). Corrigé en isolant `verifyOtp()` sur un client Supabase jetable dans `mintSupabaseSession` (`lib/data/authServer.ts`) — `admin` reste service_role du début à la fin de la requête. Testé de bout en bout : login, refresh, génération de code admin, écriture `/stores` sous RLS, trigger d'audit, et refus RLS + 403 pour un non-admin — tous vérifiés en conditions réelles contre le projet Supabase lié.
- 2026-08-06 — Lot 2 : l'accueil du portail responsable devient « À voir en priorité » (liste ordonnée par `score_priorite`, calculé en SQL). Aucun score n'est affiché : l'ordre porte l'information, chaque ligne dit « En retard de N jours » / « Vu il y a N jours ».
- 2026-08-06 — Une vue Postgres n'a pas de clé étrangère : PostgREST ne peut pas joindre `v_store_dette` à `stores`. Deux lectures + assemblage par `Map` dans `lib/data/dette.ts` (assemblage, pas calcul — la règle « le front n'agrège rien » est tenue).
- 2026-08-06 — Ajout de `date-fns-tz` (léger, compagnon de `date-fns` déjà présent) : la règle « stockage UTC / affichage Europe/Brussels » ne peut pas être tenue à la main sans réintroduire des bugs d'heure d'été. Tout le formatage passe par `src/lib/dates.ts`.
- 2026-08-06 — Barre de navigation responsable à 6 entrées (À voir · Semaine · Magasins · Signalements · Chiffres · Réglages). Équipe et absences sont rangées sous Réglages : un novice ne doit pas arbitrer entre 8 entrées de même niveau.
- 2026-08-06 — `src/lib/data/seedUsers.ts` supprimé : reliquat du dev-auth, plus référencé nulle part.
- 2026-08-06 — Lot 3 : la semaine se remplit d'un seul bouton (« Remplir la semaine »), jamais écran vide. L'heuristique est celle décidée : le magasin le plus prioritaire ouvre la journée, les arrêts suivants sont ses plus proches voisins **parmi les 25 magasins encore les plus prioritaires** — la géographie ordonne, elle ne choisit pas. Aucun solveur.
- 2026-08-06 — Le remplissage ne touche jamais une journée déjà remplie (idempotent par journée) : « Compléter la semaine » ajoute, ne réécrit pas. Undo global 10 s sur le remplissage, undo 10 s sur chaque retrait.
- 2026-08-06 — Disponibilité (absences + jours fériés) filtrée côté client à partir des tables `absences` / `public_holidays`. `jours_travailles()` reste la vérité pour la capacité agrégée ; l'appeler par personne-jour ferait 30 allers-retours pour une semaine. Filtrage de données, pas de calcul métier.
- 2026-08-06 — Un magasin = UN responsable. La table `store_assignments` autorise plusieurs porteurs ; attribuer remplace l'attribution précédente, sinon deux commerciaux planifient le même magasin le même jour.
- 2026-08-06 — Écran « Qui s'occupe de quoi » (/perimetres) livré avec le Lot 3 : sans périmètres, `store_assignments` est vide au seed et « Remplir la semaine » ne produit rien. Attribution en lot sur le filtre courant (185 magasins ne se cliquent pas un par un).
- 2026-08-06 — Lot 4 : outbox hors-ligne en IndexedDB (`lib/data/idb.ts` + `lib/data/outbox.ts`), ~200 lignes plutôt qu'une dépendance. Trois magasins : `outbox` (mutations en attente), `blobs` (photos compressées), `cache` (tournée du jour). Ordre d'écriture garanti ; une opération qui échoue bloque la file au lieu d'être doublée par les suivantes.
- 2026-08-06 — La file redemande une session Supabase (`assurerSession`) AVANT de rejouer : après une coupure, la session JWT a expiré et RLS rejetterait tout, ce qui bloquerait la file sur une erreur qui n'est pas la sienne.
- 2026-08-06 — `SessionProvider` affiche désormais l'identité en cache immédiatement puis rafraîchit en silence : la tournée du jour ne doit pas attendre le réseau (contrainte « cache avant réseau »).
- 2026-08-06 — Compte rendu (forme du `visits.report jsonb` fixée) : `message_centrale`, `ruptures`, `photos`. `rayon_conforme` et `notes` restent des colonnes réelles (filtrage admin). Seule réponse obligatoire : l'état du rayon — un formulaire à dix champs obligatoires n'est pas rempli sur le terrain.
- 2026-08-06 — Géolocalisation : une seule mesure au check-in, refus sans conséquence, annonce en clair sous le bouton (CCT n°81). Aucun `watchPosition` dans le code.
- 2026-08-06 — Service worker limité à la coquille (navigations + `_next/static`). Les données authentifiées ne passent jamais par le cache HTTP : elles vivent dans IndexedDB. Les routes `/api/` et l'origine Supabase sont explicitement exclues.
- 2026-08-06 — Migration 00009 : création du bucket privé `visit-photos` (absent jusqu'ici — le premier envoi de photo aurait échoué) + policies Storage basées sur le `visit_id` porté par le premier segment du chemin.
- 2026-08-06 — Migration 00009 : ajout de `stores_read_tournee`. La policy de périmètre seule rendait illisible un magasin confié en rattrapage (nom, adresse et téléphone invisibles sur le terrain). La lecture est désormais aussi accordée sur les magasins où l'on a une visite active.
