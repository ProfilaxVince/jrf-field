/**
 * Types générés par Supabase.
 * Lot 1 : exécuter `supabase gen types typescript --local > src/lib/data/database.types.ts`
 * après avoir provisionné le projet Supabase et validé le schéma.
 */
export type Database = unknown;
